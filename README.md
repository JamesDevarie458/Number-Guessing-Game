# Number-Guessing-Game

This a basic number guessing game between the user and the computer.

The user is introduced and is asked to enter their name,

then the program welcomes the user then prompts for a number between 1 through 10.

The program stores the number in a variable and then generates its own number.